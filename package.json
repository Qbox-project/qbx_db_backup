{
  "name": "qbx_db_backup",
  "version": "1.1.0",
  "private": true,
  "description": "Local database backups for the Qbox dashboard",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/Qbox-project/qbx_db_backup.git"
  },
  "bugs": "https://github.com/Qbox-project/qbx_db_backup/issues",
  "homepage": "https://github.com/Qbox-project/qbx_db_backup",
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/server.js && esbuild src/cli.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/cli.js && node -e \"require('fs').writeFileSync('dist/package.json', JSON.stringify({type:'commonjs'}))\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "version:set": "node scripts/set-version.mjs"
  },
  "dependencies": {
    "archiver": "^8.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "@types/archiver": "^8.0.0",
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "typescript": "~6.0.3",
    "@types/bun": "1.3.14"
  }
}
